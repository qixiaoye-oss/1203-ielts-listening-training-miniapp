<text
  class="sentence-text {{active ? 'sentence-text-active' : ''}}"
  bindtap="handleTap"
  id="{{textId}}"
  data-idx="{{index}}"
>{{content}}<text
    class="narrator {{narratorSex == '1' ? 'narrator-man' : 'narrator-woman'}}"
    wx:if="{{narrator}}"
  >{{narrator}}</text></text>
